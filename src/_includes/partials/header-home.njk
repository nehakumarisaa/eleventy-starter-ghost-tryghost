<header class="site-home-header">
    {% include 'partials/header-background.njk' %}
        <div class="inner">
            {% include 'partials/navigation.njk' %}
            <div class="site-header-content">
                 <h1 class="site-title">
                    {% if site.logo %}
                        <img class="site-logo" src="{{ site.logo }}" alt="{{ site.title }}" />
                    {% else %}
                        {{ site.title }}
                    {% endif %}
                </h1>
                <h2 class="site-description">{{ site.description }}</h2>
            </div>
        </div>
		<div class="reading-progress-bar" style="background:black;"><h2 id="bottomHeader" style="color:white">This is introduction text. Hello My name is Dr. Ritesh Reddy </h2> </div>
   
</header>
